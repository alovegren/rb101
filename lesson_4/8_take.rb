arr = [1, 2, 3, 4, 5]
arr.take(2)

# We find one signature for Array#take telling us that the method requires one parameter n (a non-negative Integer) and returns a new array containing the first n elements of self. 

# It is not destructive.